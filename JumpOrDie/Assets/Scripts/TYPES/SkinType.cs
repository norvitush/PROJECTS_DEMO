using System;

namespace VOrb
{
    [Serializable]
    public enum SkinType
    {
        basic = 0,          // даётся сразу
        achievable = 1,     // за прохождение
        rare = 2,           // собираются кусочки
        epic =3             // особые события
    }
}

